# 個人株式ポートフォリオ管理Webアプリ 仕様書

## 1. 目的

日本株・米国株を対象に、自分で銘柄を登録し保有数を入力すると、配当金と損益を自動計算して一覧表示するポートフォリオ管理Webアプリを作成する。

## 2. 対象範囲

- 日本株(東証、例: `7203.T`)
- 米国株(NYSE/NASDAQ、例: `AAPL`)
- 通貨はJPY/USDが混在するため、米国株は現在の為替レートで円換算した金額も併記する

## 3. データ取得元

- Yahoo Finance系の無料API/ライブラリ(例: `yfinance`、または Yahoo Finance の query API)を利用する
- 取得する情報
  - 株価: 現在値、前日終値
  - 配当情報: 1株あたり配当金額、権利落ち日、支払日、年間の配当回数(実績ベース、直近1年分の配当履歴から算出)
  - 為替レート: USD/JPY(米国株を円換算する場合)

### 3.1 技術的な注意事項(要検討)

Yahoo Finance系のAPIはブラウザから直接fetchするとCORS制限で失敗する可能性が高い。以下いずれかの対応が必要:

- 軽量なローカルサーバー(Python/Flask や Node/Express)を用意し、`yfinance`等でデータ取得 → JSONをフロントエンドに返すプロキシとして機能させる
- CORSプロキシサービスを経由する(安定性・利用制限に注意)
- 都度手動でCSVインポートする形にフォールバックする

上記のうち「ローカルサーバーを立てて yfinance 経由で取得する」構成を推奨案とし、実装時にClaudeと相談の上で最終決定する。

## 4. 主要機能

### 4.1 銘柄登録

- 銘柄コード(例: `7203.T` / `AAPL`)を入力して検索・追加
- 追加時に銘柄名・市場・通貨を自動取得して表示

### 4.2 保有情報入力

- 保有数(株数)
- 取得単価(平均取得価格、任意入力・損益計算に使用)
- 取得日(任意)

### 4.3 株価取得・表示

- 登録銘柄の現在株価、前日比(金額・%)を一覧表示
- 更新ボタンで最新株価を再取得

### 4.4 配当情報表示

- 銘柄ごとに以下を一覧表示
  - 1株あたり配当金額(直近実績)
  - 年間配当回数
  - **配当時期(配当月)**: 直近1年分の配当実績から、権利落ちが発生した月を一覧化(例: 日本株の場合「3月・9月」、米国株の四半期配当の場合「1月・4月・7月・10月」)
  - 直近の権利落ち日・支払日(1回ごとの内訳、複数回ある場合はすべて表示)
  - 次回配当予定日(取得できる場合)
- 配当スケジュールは銘柄詳細を開くと、回次ごとに「権利落ち日 / 支払日 / 1株配当金額」の一覧(明細)で確認できるようにする

### 4.5 配当金計算

- 銘柄ごとの年間受取配当金 = 保有数 × 1株あたり配当金額 × 年間配当回数
- ポートフォリオ全体の年間配当金合計を表示
- 米国株は円換算額も併記

### 4.6 損益計算

- 銘柄ごとの評価損益 = (現在株価 − 取得単価) × 保有数
- 評価損益率(%)も表示
- ポートフォリオ全体の評価損益合計を表示

### 4.7 ポートフォリオ一覧

以下を1つのテーブルにまとめて表示する。

| 項目 | 内容 |
|---|---|
| 銘柄コード・銘柄名 | 例: 7203.T トヨタ自動車 |
| 市場・通貨 | 東証 / JPY、NASDAQ / USD など |
| 保有数 | 入力値 |
| 取得単価 | 入力値 |
| 現在株価 | 自動取得 |
| 評価額 | 現在株価 × 保有数 |
| 評価損益 / 損益率 | 自動計算 |
| 1株配当金・年間回数 | 自動取得 |
| 配当時期(配当月) | 例: 3月・9月 |
| 年間配当金見込み | 自動計算 |

ポートフォリオ全体の合計行(評価額合計・損益合計・年間配当金合計)も表示する。

## 5. データモデル(概要)

```
銘柄 {
  code: string          // 例 "7203.T", "AAPL"
  name: string
  market: string         // "東証" / "NYSE" / "NASDAQ" など
  currency: string        // "JPY" / "USD"
  currentPrice: number
  previousClose: number
  dividendPerShare: number   // 直近実績の1株配当(年間合計)
  dividendFrequency: number  // 年間回数
  dividendMonths: number[]   // 配当時期(月)例 [3, 9]
  dividendSchedule: {        // 回次ごとの配当明細
    exDividendDate: string   // 権利落ち日
    paymentDate: string      // 支払日
    amountPerShare: number   // その回の1株配当金額
  }[]
}

保有ポジション {
  stockCode: string
  quantity: number       // 保有数
  avgCost: number         // 取得単価(任意)
  acquiredDate: string    // 取得日(任意)
}
```

## 6. UI構成(想定)

1. 銘柄追加フォーム(銘柄コード入力 → 検索 → 保有数・取得単価入力 → 追加)
2. ポートフォリオ一覧テーブル(第4.7節の項目)
3. サマリーエリア(評価額合計、損益合計、年間配当金合計)
4. 更新ボタン(株価・配当情報を再取得)

データはブラウザの保存領域を使わず、アプリ内のメモリ上またはローカルファイル(JSON等)で保持する構成とする。

## 7. 実装形式

- シングルページのWebアプリ
- フロントエンドはReact(Vite構成)を採用する
  - 理由: 銘柄の動的な追加・削除、保有数入力による再計算、一覧テーブルの状態管理が中心機能であり、状態変更に応じて表示が自動更新されるReactとの相性が良いため
  - 将来的にグラフ表示・複数ポートフォリオ管理・フィルタ/ソート機能などを拡張する場合にも対応しやすい
- 外部データ取得部分は第3.1節の方針に従いローカルサーバー経由での実装を基本とする

## 8. 今後の検討事項

- データ取得元APIの最終選定(yfinanceのローカルプロキシ構成で問題ないか)
- 配当課税(源泉徴収税)を考慮した手取り配当金額を計算に含めるか
- 保有データの永続化方法(ファイル保存 / エクスポート機能の要否)
